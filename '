---
- name: with_items.yml
  hosts: all
  tasks:
  - name: install packages
    apt:
      name: "{{item}}"
      state: present

    with_items: 
    - apache2
    - nginx
    
  - name: start services
    service:
      name: "{{item}}"
      state: restarted
      enabled: true

